# Files and directories excluded from test/prod deployments.
# Used by server-side post-receive hooks to clean up after git checkout.
# Paths are relative to the work tree root.

# Claude Code / AI tooling
.claude/

# UserSpice AI dev plugins (dev tooling only — not needed on server)
usersc/plugins/ai_prompts/
usersc/plugins/db_explainer/

# GitHub (CI workflows, issue templates, bot configs)
.github/
.imgbotconfig

# Git config files (not needed on server)
.githooks/
.gitignore
.gitmessage

# Development documentation (not served to users)
docs/development/
docs/testing/
docs/releases/
docs/README.md

# Tests
tests/

# Scripts (already blocked by .htaccess, but not needed on server)
scripts/

# Linting and static analysis
eslint.config.mjs
phpstan.neon
phpstan-baseline.neon
phpstan-bootstrap.php
.markdownlint.json
.markdownlintignore
.semgrepignore

# Playwright configs
playwright.config.js
playwright.config.prod.js
playwright.config.test.js

# PHP test configs
phpunit.xml
phpunit-unit.xml
phpunit-integration.xml

# Node / JS tooling
package.json
package-lock.json
tsconfig.json

# Node dependency tree (npm ci recreates it on every deploy — .htaccess
# already blocks web access, this just shrinks the deployed docroot)
node_modules/

# Composer (dependencies not deployed via git)
composer.json
composer.lock

# Project documentation not needed on server
CLAUDE.md
README.md
SECURITY.md
LICENSE
.env.example
.env.local.sample
instructions.txt

# Database migrations (run automatically by post-receive hook, not served)
database/

# Dev-only scripts and utilities
utilities/

# Wiki source (served via GitHub wiki, not the web server)
wiki/
